let cars = ['tata','honda','maruti','toyota'];

    console.log(cars, cars.length); //4
    delete cars[1];     //only the value gets deleted and place holder is still there
    console.log(cars, cars.length); //4

